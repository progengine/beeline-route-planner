"""
Общие константы и допущения проекта.

ТЗ разрешает упрощённый расчёт расстояния/времени в пути по координатам
(раздел 3.2, строка "Карта") — с обязательным описанием допущения в README.
Здесь фиксируется допущение: расстояние по гаверсинусу (по прямой) x
поправочный коэффициент на непрямолинейность дорог, время = расстояние /
средняя скорость для типа транспорта инженера.
"""

from app.models import VehicleType

# Средняя скорость передвижения по типу транспорта, км/ч.
# Используется для упрощённого расчёта времени в пути, если не подключен
# внешний картографический сервис (OSRM / Google Directions и т.п.).
AVERAGE_SPEED_KMH: dict[VehicleType, float] = {
    VehicleType.CAR: 30.0,
    VehicleType.PUBLIC_TRANSPORT: 20.0,
    VehicleType.BICYCLE: 15.0,
    VehicleType.WALKING: 5.0,
}

# Поправочный коэффициент "прямая -> дорожная сеть" для расстояния по
# гаверсинусу (реальный путь обычно длиннее прямой).
ROAD_DISTANCE_FACTOR: float = 1.3

# Диспетчерские ограничения по объёму данных (раздел 6 ТЗ).
MAX_ENGINEERS: int = 15
MAX_REQUESTS: int = 100

# Единицы времени, используемые везде внутри решателя: минуты от полуночи.
MINUTES_IN_DAY: int = 24 * 60
